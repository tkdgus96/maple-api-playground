import { Route, Routes } from "react-router-dom";

const Router = () => {
  return (
    <>
      <Routes>
        <Route path="character"></Route>
      </Routes>
    </>
  );
};

export default Router;
